# LearnModeView

Static LearnMode webpage containing the initial locked learning card and active Processing Elements question card.

The initial two cards are intentionally marked with stable `data-card-id` values so future cards can be added without changing these unless requested.
